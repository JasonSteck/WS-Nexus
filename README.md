# WS-Nexus
WebSocket Nexus is a Node app that acts as a Web Socket Proxy which allows browser windows to emulate Client/Server Models.

To put it simply, it lets your browser talk with browsers on other devices.
